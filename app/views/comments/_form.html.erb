<%= form_for ([@artwork, @artwork.comments.build]) do |f| %>
<h4>
  <%=f.label :comment %>
  <%=f.text_area :content %>
</h4>
<p>
  <div><%= f.button :submit, class: "btn btn-primary" %></div>
</p>
    <p class="alert"><%= alert %></p>
<% end %>